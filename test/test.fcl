thread "otto"
{
	count(250,20);
}
thread "willi"
{
	count(500,10);
}
client "einsatzwagen"
{
	start("willi","hugo");
}
fire
{
	writeLine(getType("test"));
	var pics = loadPicture("test/pic.txt");
	writeLine(pics);
	for (pic in pics)
	{
		writeLine(pic);
	}
	var positions = loadPosition("test/pos.txt");
	for (pos in positions)
	{
		writeLine(pos);
	}
	start("willi","hugo");
	start("otto","anton");

	var i = 1;
	writeLine("i: ", i);

	startClient("einsatzwagen");
}

